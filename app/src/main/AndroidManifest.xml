<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.haozi.idscaner2016">

    <!-- 屏幕唤醒锁权限 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.RESTART_PACKAGES" />
    <!-- cache功能需要读写外部存储器 -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!--允许应用读取低级别的系统日志文件 -->
    <uses-permission android:name="android.permission.READ_LOGS"></uses-permission>
    <!-- 使用网络功能所需权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-feature android:name="android.hardware.usb.host" />
    <uses-permission android:name="android.hardware.usb.accessory" />
    <uses-permission android:name="android.permission.DEVICE_POWER" tools:ignore="ProtectedPermissions" />

    <application
        android:name=".common.app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar">

        <activity android:name=".client.ui.wellcome.WellcomeActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".client.ui.home.HomeNewActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>

        <activity
            android:name=".client.ui.home.HomeActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>

        <activity
            android:name=".client.ui.home.RecordSearchActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>

        <activity
            android:name=".client.ui.home.RecordSumActivity"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>

        <activity
            android:name=".client.ui.home.SignActivity"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>

        <activity
            android:name=".client.ui.home.UnitManageActivity"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>

        <activity
            android:name=".client.ui.home.CodeScanActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" >
            <!--android:screenOrientation="landscape"-->
        </activity>

        <activity
            android:name=".client.ui.home.PrintBillActivity"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>

        <activity
            android:name=".gpringter.GPrinterConnectDialog"
            android:label="@string/port_configuration"
            android:theme="@android:style/Theme.Holo.Light.Dialog"
            android:configChanges="keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_DETACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <!--蓝牙打印机-->
        <activity
            android:name="com.gprinter.printer.GprinterMainActivity"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan|stateHidden" >
        </activity>
        <!-- 端口设置对话 -->
        <activity
            android:name="com.gprinter.printer.PrinterConnectDialog"
            android:label="@string/port_configuration"
            android:theme="@android:style/Theme.Holo.Light.Dialog"
            android:configChanges="keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_DETACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <!-- 蓝牙设备 -->
        <activity
            android:name="com.gprinter.printer.BluetoothDeviceList"
            android:label="@string/bluetooth_device_list"
            android:theme="@android:style/Theme.Holo.Light.Dialog"
            android:configChanges="keyboardHidden|orientation|screenSize">
        </activity>
        <!-- 端口设置对话 -->
        <activity
            android:name="com.gprinter.printer.PortConfigurationActivity"
            android:label="@string/port_configuration"
            android:theme="@android:style/Theme.Holo.Light.Dialog"
            android:configChanges="keyboardHidden|orientation|screenSize" >
        </activity>
        <!-- USB设备 -->
        <activity
            android:name="com.gprinter.printer.UsbDeviceList"
            android:label="@string/usb_devices_list"
            android:theme="@android:style/Theme.Holo.Light.Dialog"
            android:configChanges="keyboardHidden|orientation|screenSize"  >
        </activity>

        <receiver android:name="com.gprinter.printer.DeviceBootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.gprinter.service.GpPrintService"
            android:label="GpPrintService"
            android:process=":remote"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.gprinter.aidl.GpPrintService" />
            </intent-filter>
        </service>

    </application>

</manifest>